# Venv della macchina installazione — C:\gaia\venv, condiviso con
# minipc/installation/ (stesso schema di minipc/touchdesigner/requirements.txt,
# ma qui è lo stesso venv del resto della macchina, non uno dedicato).
# Installa con:
#   C:\gaia\venv\Scripts\pip.exe install -r requirements.txt

paho-mqtt
python-osc
