# Claude Code sessions (privato)
.claude/

# Python
venv/
venv
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# ── Pi scripts — modelli e binary scaricati da install.sh ─────────────────────
pi/yolo/models/
pi/mediapipe/models/
pi/voice/models/
ops/models/
ops/voice/models/
pi/voice/bin/

# Pi agent — device.json è Pi-specifico (generato da install.sh dal template)
pi/agent/device.json

# Cartelle venv Pi (non devono finire nel repo)
*/venv/

# File temporanei OTA
*.ota_tmp

# ── miniPC — dati personali e runtime ─────────────────────────────────────────
# Dati personali voice enrollment
minipc/script/voice_db.json

# Calibrazione mic salvata automaticamente da gaia_listener.py ad ogni tuning
# da admin.html — non è config editoriale, cambia da sola
minipc/script/listener_config.json

# Wakeword models (file grandi)
minipc/wakeword_models/

# Campioni voce "Gaia" + modello dedicato al mic del miniPC — dati personali
# (registrazioni vocali), stessa categoria di voice_db.json
minipc/script/gaia_wakeword_samples_minipc/
minipc/script/gaia_wakeword_samples_ops/
minipc/script/gaia_wakeword_samples/

# Local agent runtime (device-specific, generato automaticamente)
minipc/local_agent.lock
minipc/local_agent_config.json
ops/agent/agent.lock
ops/agent/agent_config.json
ops/agent/agent.log

# ── Shared ────────────────────────────────────────────────────────────────────
# Configurazioni locali con IP/credenziali
/etc/gaia/

# Log e dati runtime
*.log
mosquitto/log/
mosquitto/data/

# Backup locali
backups/

# Editor
.vscode/
*.swp
*.swo
.DS_Store

# Node-RED runtime (solo flows.json ci interessa)
.node-red/

# Ollama / OpenHAB (servizi separati)
ollama/
openhab/

# Campioni audio citofono (dati runtime, non in git)
minipc/script/doorbell_samples/

# Campioni audio wakeword e modelli addestrati (dati personali, non in git)
minipc/script/gaia_wakeword_samples/*.wav
minipc/script/gaia_wakeword_samples/positive/
minipc/script/gaia_wakeword_samples/negative/
minipc/script/gaia_wakeword_samples/doorbell/
minipc/script/gaia_wakeword_samples/*.pkl
pi/agent/gaia_core.json
minipc/script/provision_registry.json
web/*.bak*
