# Captive portal DNS per l'hotspot Gaia-Setup.
# Installato in /etc/NetworkManager/dnsmasq-shared.d/gaia-captive.conf:
# vale SOLO per le connessioni NM in modalità "shared" (cioè il nostro
# hotspot) — non tocca il DNS normale del Pi.
# Ogni nome risolve all'IP del portale → i telefoni aprono il popup captive.
address=/#/10.42.0.1
