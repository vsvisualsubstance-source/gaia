# Venv miniPC — ~/core-node-0/venv (symlink → /media/core/D/venv)
# Usato da: minipc/script/gaia_listener.py, enroll_voice.py, transcribe.py
#
# Installa con:
#   source ~/core-node-0/venv/bin/activate
#   pip install -r requirements.txt

# Core
numpy==2.4.6
scipy
paho-mqtt
soundfile==0.13.1

# Audio / VAD (usare webrtcvad-wheels su Python 3.12+)
pyaudio==0.2.14
webrtcvad==2.0.10

# STT
faster-whisper==1.2.1

# Speaker ID
resemblyzer

# Wakeword (opzionale sul miniPC)
openwakeword

# PyTorch — richiesto da resemblyzer e openwakeword
torch==2.12.0+cu130

# Vision — usato da script locali (non da gaia_listener)
mediapipe==0.10.35
opencv-python==4.13.0.0
